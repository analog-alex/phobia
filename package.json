{
  "name": "phobia",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "assets:optimize": "node scripts/optimize-enemy-assets.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write ."
  },
  "dependencies": {
    "@babylonjs/core": "^8.0.0",
    "@babylonjs/loaders": "^8.56.2"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@gltf-transform/cli": "^4.4.0",
    "typescript": "^5.8.0",
    "vite": "^6.2.0"
  }
}
